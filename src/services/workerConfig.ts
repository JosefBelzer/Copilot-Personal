/**
 * Shared configuration for Worker communication.
 *
 * The Worker URL is defined in ONE place to prevent drift between files.
 */
export const WORKER_URL = "https://copilot-personal-worker.copilot-personal.workers.dev";
